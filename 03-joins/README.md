# 03 - Joins

## Concepts covered
- INNER JOIN (only matching rows)
- LEFT JOIN (all left rows, matched or not)
- Simulating RIGHT/FULL JOIN in SQLite (not natively supported - shown two ways)
- Self joins (joining a table to itself)

## Files
- `schema.sql`
- `queries.sql`
- `run.js`
