# 06 - Schema Design & Normalization

## Concepts covered
- 1NF, 2NF, 3NF explained with a BEFORE/AFTER example
- Why duplication in a database is dangerous (update anomalies)
- Designing a real multi-table e-commerce-style schema

## Files
- `bad-schema.sql` — a single denormalized table (the "beginner mistake")
- `good-schema.sql` — the same data properly normalized into related tables
- `explanation.md` — walks through WHY each change was made
