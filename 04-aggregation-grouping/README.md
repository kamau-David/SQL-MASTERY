# 04 - Aggregation & Grouping

## Concepts covered
- Aggregate functions: COUNT, SUM, AVG, MIN, MAX
- GROUP BY - collapsing rows into summary groups
- HAVING - filtering AFTER aggregation (vs WHERE, which filters BEFORE)

## Files
- `schema.sql`
- `queries.sql`
- `run.js`
