# 11 - SQL Injection & Security

## Concepts covered
- What SQL injection actually is, demonstrated with real (safely sandboxed) code
- Parameterized queries as the fix - always, no exceptions
- Principle of least privilege (DB users/permissions)

## Files
- `run.js` — shows the vulnerable version breaking, then the fixed version

## Run it
```bash
npm install
node run.js
```
